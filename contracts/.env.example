# Polygon Amoy testnet (chain id 80002). Never commit real keys.
# Public RPC is rate-limited; a provider URL (Alchemy/Infura) is more reliable.
AMOY_RPC_URL=https://rpc-amoy.polygon.technology
AMOY_PRIVATE_KEY=

# After a successful deploy, copy the printed address into the backend .env
# (Web3.py wiring is a later slice):
# DATACHAIN_CONTRACT_ADDRESS=
